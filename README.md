# AI Interview App

An AI-powered interview application built with React, LiveKit, Python, Gemini, Deepgram, ElevenLabs, and the LiveAvatar plugin.

## Project Structure
- `/frontend`: React + Vite application.
- `/backend`: Python + FastAPI LiveKit Agent server.

See `plan.md` for the full implementation strategy and milestones.